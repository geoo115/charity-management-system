# ZAP Configuration File for API Security Testing

# Global Configuration
zap.api.disablekey=false
zap.api.incerrordetails=false

# Authentication Configuration
authentication.method=form
authentication.loginurl=http://localhost:8080/api/v1/auth/login
authentication.username=admin@test.com
authentication.password=testpassword

# Session Management
session.token.name=Authorization
session.token.value=Bearer

# Security Rules Configuration
rules.cookie.secure=FAIL
rules.cookie.httponly=FAIL
rules.header.xframe=FAIL
rules.header.xcontent=FAIL
rules.header.xss=FAIL
rules.sql.injection=FAIL
rules.path.traversal=FAIL

# Custom Rules
custom.sql.patterns=(?i)(union|select|insert|delete|update|drop|create|alter|exec|execute)
custom.xss.patterns=(?i)(<script|javascript:|onerror|onload|alert\(|prompt\(|confirm\()

# Endpoints to Test
target.endpoints=/api/v1/auth/login,/api/v1/auth/register,/api/v1/help-requests,/api/v1/donations

# Exclusions
exclude.paths=/health,/metrics,/favicon.ico